using System;

namespace WeldAdminPro.UI
{
    // Optional Program.cs — WPF will normally use App.xaml as startup, but this file
    // can be used to run the app explicitly if you prefer using a Program entry point.
    public static class Program
    {
        [STAThread]
        public static void Main(string[] args)
        {
            var app = new App();
            app.InitializeComponent();
            app.Run();
        }
    }
}
