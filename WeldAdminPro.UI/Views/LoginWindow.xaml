<Window x:Class="WeldAdminPro.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WeldAdminPro.UI.ViewModels"
        mc:Ignorable="d"
        Title="WeldAdmin Pro - Login"
        Height="320"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Loaded="LoginWindow_Loaded">

    <!-- NOTE: DataContext intentionally NOT set here. It will be provided by DI in App.xaml.cs -->

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to WeldAdmin Pro" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,6" Orientation="Vertical">
            <Label Content="Username"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="320"/>

            <Label Content="Password" Margin="0,8,0,0"/>
            <!-- PasswordBox cannot bind to ViewModel securely; we'll forward it from code-behind -->
            <PasswordBox x:Name="PwdBox" Width="320" PasswordChanged="PwdBox_PasswordChanged"/>

            <CheckBox Content="Remember me" IsChecked="{Binding RememberMe}" Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Login" Width="100" Margin="6,0" Command="{Binding LoginCommand}"/>
            <Button Content="Cancel" Width="100" Margin="6,0" Click="Cancel_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Red" Margin="2,12,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>
