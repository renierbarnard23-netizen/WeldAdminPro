<UserControl x:Class="WeldAdminPro.UI.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">

        <DataGrid
            ItemsSource="{Binding Logs}"
            AutoGenerateColumns="False"
            IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn
                    Header="Time"
                    Binding="{Binding Timestamp}"
                    Width="180"/>

                <DataGridTextColumn
                    Header="User"
                    Binding="{Binding Username}"
                    Width="150"/>

                <DataGridTextColumn
                    Header="Action"
                    Binding="{Binding Action}"
                    Width="180"/>

                <DataGridTextColumn
                    Header="Module"
                    Binding="{Binding Module}"
                    Width="180"/>

                <DataGridTextColumn
                    Header="Details"
                    Binding="{Binding Details}"
                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>