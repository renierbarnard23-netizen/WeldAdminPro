<Window x:Class="WeldAdminPro.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WeldAdminPro.UI.ViewModels"
        Title="WeldAdminPro" Height="600" Width="900">
    <!-- Move InputBindings here (was incorrectly on Grid) -->
    <Window.InputBindings>
        <!-- example keybindings - ensure these match commands on your MainViewModel -->
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding NewProjectCommand}" />
        <KeyBinding Key="F" Modifiers="Control" Command="{Binding FocusSearchCommand}" />
    </Window.InputBindings>

    <Grid>
        <!-- Rest of your layout -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left navigation -->
        <ListBox x:Name="NavList"
                 ItemsSource="{Binding NavItems}"
                 SelectedItem="{Binding SelectedNavItem, Mode=TwoWay}"
                 DisplayMemberPath="Title"
                 Grid.Column="0" />

        <!-- Content -->
        <ContentControl Content="{Binding SelectedView}" Grid.Column="1" />
    </Grid>
</Window>
