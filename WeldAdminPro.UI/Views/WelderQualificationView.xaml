<UserControl x:Class="WeldAdminPro.UI.Views.Quality.WelderQualificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WeldAdminPro.UI.ViewModels">

    <UserControl.DataContext>
        <vm:WelderQualificationViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welder Qualifications"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Welders}"
                  SelectedItem="{Binding SelectedWelder}"
                  AutoGenerateColumns="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Welder" Binding="{Binding WelderName}"/>
                <DataGridTextColumn Header="Process" Binding="{Binding Process}"/>
                <DataGridTextColumn Header="Material" Binding="{Binding MaterialGroup}"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Position}"/>
                <DataGridTextColumn Header="Thickness" Binding="{Binding ThicknessDisplay}" />
                <DataGridTextColumn Header="Max t" Binding="{Binding ThicknessMax}"/>
                <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryDate}"/>

                <DataGridTemplateColumn Header="Status">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=., Converter={StaticResource WelderStatusConverter}, FallbackValue=''}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">

            <Button Content="Add"
                    Command="{Binding AddWelderCommand}"
                    Margin="5"/>

            <Button Content="Delete"
                    Command="{Binding DeleteWelderCommand}"
                    Margin="5"/>

        </StackPanel>

    </Grid>
</UserControl>