<UserControl x:Class="WeldAdminPro.UI.Views.ProjectCostDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

	<Grid Margin="20">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Project Material Cost Dashboard"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

			<DataGrid Grid.Row="1"
          			ItemsSource="{Binding ProjectCosts}"
          			AutoGenerateColumns="False"
          			IsReadOnly="True"
          			MouseDoubleClick="ProjectGrid_DoubleClick">

			<DataGrid.Columns>

				<DataGridTextColumn Header="Project"
                                    Binding="{Binding ProjectName}"
                                    Width="2*" />

				<DataGridTextColumn Header="Units Consumed"
                                    Binding="{Binding TotalUnitsConsumed}"
                                    Width="*" />

				<DataGridTextColumn Header="Material Cost"
                                    Binding="{Binding TotalMaterialCost, StringFormat=R {0:N2}}"
                                    Width="*" />

			</DataGrid.Columns>

		</DataGrid>

	</Grid>

</UserControl>