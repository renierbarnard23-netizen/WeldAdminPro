<Window x:Class="WeldAdminPro.UI.Views.NewProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Height="220"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Project Number" FontWeight="Bold"/>
        <TextBox Grid.Row="1"
                 Text="{Binding ProjectNumber}"
                 IsReadOnly="True"
                 Margin="0,5,0,10"/>

        <TextBlock Grid.Row="2" Text="Project Name" FontWeight="Bold"/>
        <TextBox
    Grid.Row="1"
    Text="{Binding Project.ProjectNumber}"
    IsReadOnly="True"
    Margin="0,5,0,10"/>

<TextBox
    Grid.Row="3"
    Text="{Binding Project.ProjectName, UpdateSourceTrigger=PropertyChanged}"
    Margin="0,5,0,0"/>


        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button
    Content="Save"
    Width="80"
    Margin="5"
    Command="{Binding SaveCommand}" />


            <Button
    Content="Cancel"
    Width="80"
    Margin="5"
    Command="{Binding CancelCommand}" />

        </StackPanel>
    </Grid>
</Window>
