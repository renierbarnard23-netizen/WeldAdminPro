<Window x:Class="WeldAdminPro.UI.Views.DocumentVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Document Vault"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen">

	<Grid Margin="20">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- HEADER -->
		<TextBlock Text="QA Document Vault"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

		<!-- BUTTON BAR -->
		<StackPanel Grid.Row="1"
					Orientation="Horizontal"
					Margin="0,0,0,15">

			<Button x:Name="UploadButton"
					Content="Upload Document"
					Width="160"
					Height="35"
					Margin="0,0,10,0"
					Click="UploadDocument_Click"/>

			<Button x:Name="OpenButton"
					Content="Open Document"
					Width="160"
					Height="35"
					Margin="0,0,10,0"
					Click="OpenDocument_Click"/>

			<Button x:Name="DeleteButton"
					Content="Delete Document"
					Width="160"
					Height="35"
					Click="DeleteDocument_Click"/>

		</StackPanel>

		<!-- DOCUMENT GRID -->
		<DataGrid x:Name="DocumentsGrid"
				Grid.Row="2"
				ItemsSource="{Binding Documents}"
				AutoGenerateColumns="False"
				IsReadOnly="True"
				MouseDoubleClick="DocumentsGrid_MouseDoubleClick">

			<DataGrid.Columns>

				<DataGridTextColumn Header="File Name"
                                    Binding="{Binding FileName}"
                                    Width="250"/>

				<DataGridTextColumn Header="Category"
                                    Binding="{Binding Category}"
                                    Width="120"/>

				<DataGridTextColumn Header="Revision"
                                    Binding="{Binding Revision}"
                                    Width="80"/>

				<DataGridTextColumn Header="Uploaded By"
                                    Binding="{Binding UploadedBy}"
                                    Width="150"/>

				<DataGridTextColumn Header="Uploaded Date"
                                    Binding="{Binding UploadedDate}"
                                    Width="180"/>

				<DataGridCheckBoxColumn Header="Approved"
                                        Binding="{Binding IsApproved}"
                                        Width="100"/>

				<DataGridTextColumn Header="Description"
                                    Binding="{Binding Description}"
                                    Width="*"/>

			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</Window>
