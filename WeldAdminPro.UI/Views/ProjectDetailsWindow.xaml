<Window x:Class="WeldAdminPro.UI.Views.ProjectDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Project Details"
        Height="300"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Project Number -->
        <TextBlock Text="Project Number" FontWeight="Bold"/>
        <TextBox Grid.Row="1"
                 Text="{Binding ProjectNumber}"
                 IsReadOnly="True"
                 Margin="0,5,0,10"/>

        <!-- Project Name -->
        <TextBlock Grid.Row="2" Text="Project Name" FontWeight="Bold"/>
        <TextBox Grid.Row="3"
                 Text="{Binding ProjectName}"
                 Margin="0,5,0,10"/>

        <!-- Dates -->
        <StackPanel Grid.Row="4" Orientation="Vertical">
            <TextBlock Text="Start Date" FontWeight="Bold"/>
            <DatePicker SelectedDate="{Binding StartDate}" Margin="0,5,0,10"/>

            <TextBlock Text="End Date" FontWeight="Bold"/>
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0,5,0,10"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Save"
                    Width="80"
                    Margin="5"
                    Command="{Binding SaveCommand}"/>
            <Button Content="Cancel"
                    Width="80"
                    Margin="5"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
