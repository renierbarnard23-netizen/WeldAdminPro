<Window x:Class="WeldAdminPro.UI.Views.Quality.AddHoldPointWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Hold Point"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Hold Point Type"/>

        <ComboBox Grid.Row="1"
                  Margin="0,5,0,15"
                  ItemsSource="{Binding HoldPointTypes}"
                  SelectedItem="{Binding SelectedType}"/>

        <CheckBox Grid.Row="2"
                  Content="Mandatory"
                  IsChecked="{Binding IsMandatory}"
                  Margin="0,0,0,15"/>

        <TextBox Grid.Row="3"
                 Text="{Binding Comments}"
                 AcceptsReturn="True"/>

        <Button Grid.Row="4"
                Content="Add"
                Width="120"
                Height="35"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Command="{Binding SaveCommand}"/>

    </Grid>
</Window>