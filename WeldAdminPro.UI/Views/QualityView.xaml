<UserControl x:Class="WeldAdminPro.UI.Views.QualityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">

        <StackPanel>

            <!-- 🔥 PROJECT STATUS -->
            <TextBlock Text="{Binding ProjectStatus}"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{Binding ProjectStatusColor}"
                       Margin="0,0,0,10"/>

            <!-- 🔥 DATA GRID -->
            <DataGrid ItemsSource="{Binding Documents}" AutoGenerateColumns="False">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>

                            <DataTrigger Binding="{Binding IsUploaded}" Value="False">
                                <Setter Property="Background" Value="#FFCDD2"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding IsUploaded}" Value="True">
                                <Setter Property="Background" Value="#C8E6C9"/>
                            </DataTrigger>

                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Document"
                                        Binding="{Binding DocumentType}" Width="*" />

                    <DataGridCheckBoxColumn Header="Required"
                                            Binding="{Binding IsRequired}" />

                    <DataGridCheckBoxColumn Header="Uploaded"
                                            Binding="{Binding IsUploaded}" />

                    <DataGridTextColumn Header="File"
                                        Binding="{Binding FilePath}" Width="2*" />

                    <!-- 🔥 ACTION COLUMN -->
                    <DataGridTemplateColumn Header="Action">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">

                                    <Button Content="Upload" Command="{Binding DataContext.UploadCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />

                                    <Button Content="Open" Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Item}" />

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>

        </StackPanel>

    </Grid>

</UserControl>