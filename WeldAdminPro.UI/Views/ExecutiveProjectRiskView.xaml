<UserControl x:Class="WeldAdminPro.UI.Views.ExecutiveProjectRiskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

	<Grid Margin="10">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<TextBlock Text="Project Risk Dashboard"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

		<!-- Risk Grid -->
		<DataGrid Grid.Row="1"
                  ItemsSource="{Binding Projects}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Margin="0,5,0,0">

			<DataGrid.Columns>

				<DataGridTextColumn Header="Project"
                        Binding="{Binding ProjectName}"
                        Width="*"/>

				<DataGridTextColumn Header="Budget"
                        Binding="{Binding Budget, StringFormat=N2}"
                        Width="150"/>

				<DataGridTextColumn Header="Material Cost"
                        Binding="{Binding MaterialCost, StringFormat=N2}"
                        Width="150"/>

				<DataGridTextColumn Header="Budget Used %"
                        Binding="{Binding BudgetUsedPercent, StringFormat=N2}"
                        Width="150"/>

				<DataGridTemplateColumn Header="Risk" Width="120">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Border Padding="4" CornerRadius="4">
								<Border.Style>
									<Style TargetType="Border">
										<Setter Property="Background" Value="LightGreen"/>
										<Style.Triggers>

											<DataTrigger Binding="{Binding RiskLevel}" Value="Medium">
												<Setter Property="Background" Value="Khaki"/>
											</DataTrigger>

											<DataTrigger Binding="{Binding RiskLevel}" Value="High">
												<Setter Property="Background" Value="Orange"/>
											</DataTrigger>

											<DataTrigger Binding="{Binding RiskLevel}" Value="Critical">
												<Setter Property="Background" Value="Tomato"/>
											</DataTrigger>

										</Style.Triggers>
									</Style>
								</Border.Style>

								<TextBlock Text="{Binding RiskLevel}"
                               HorizontalAlignment="Center"
                               FontWeight="Bold"/>
							</Border>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

			</DataGrid.Columns>

		</DataGrid>

	</Grid>

</UserControl>