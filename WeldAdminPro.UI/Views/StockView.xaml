<UserControl x:Class="WeldAdminPro.UI.Views.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Width="953">

	<TabControl Margin="10">

		<!-- ===================== -->
		<!-- TAB 1: STOCK ITEMS -->
		<!-- ===================== -->
		<TabItem Header="Stock Items">

			<DockPanel Width="875">

				<!-- ===================== -->
				<!-- TOP TOOLBAR -->
				<!-- ===================== -->
				<Grid DockPanel.Dock="Top" Margin="0,0,0,10">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<!-- LEFT: ACTION BUTTONS -->
					<StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">

						<Button Content="New"
                                Width="54"
                                Margin="0,0,5,0"
                                Command="{Binding NewItemCommand}" />

						<Button Content="Edit"
                                Width="48"
                                Margin="0,0,5,0"
                                Command="{Binding EditItemCommand}" />

						<Button Content="Stock IN"
                                Width="62"
                                Margin="0,0,5,0"
                                Command="{Binding StockInCommand}" />

						<Button Content="Stock OUT"
                                Width="75"
                                Margin="0,0,5,0"
                                Command="{Binding StockOutCommand}" />

						<Button Content="Delete"
                                Width="58"
                                Margin="0,0,5,0"
                                Command="{Binding DeleteStockItemCommand}"
                                Background="LightGray"
                                ToolTip="Cannot delete items with transaction history" />

						<Button Content="Undo"
                                Width="45"
                                Margin="0,0,5,0"
                                Command="{Binding UndoEditCommand}"
                                Background="LightGray" />

						<Button Content="Categories"
                                Width="78"
                                Margin="0,0,5,0"
                                Click="ManageCategories_Click" />

						<Button Content="History"
                                Width="62"
                                Command="{Binding ViewHistoryCommand}" />
					</StackPanel>

					<!-- CENTER: SEARCH -->
					<StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>

					<!-- RIGHT: FILTERS -->
					<StackPanel Grid.Column="2"
            Orientation="Horizontal"
            VerticalAlignment="Center"/>
					<CheckBox Content="Low stock only"
              VerticalAlignment="Center"
              Margin="5,0,135,0"
              IsChecked="{Binding ShowLowStockOnly}" Grid.Column="1" />
					<Button Content="Export Low Stock"
            Command="{Binding ExportLowStockCommand}" Grid.Column="1" Margin="145,0,93,0" />

				</Grid>

				<!-- ===================== -->
				<!-- MAIN CONTENT -->
				<!-- ===================== -->
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="200"/>
						<ColumnDefinition Width="497*"/>
						<ColumnDefinition Width="96*"/>
						<ColumnDefinition Width="82*"/>
					</Grid.ColumnDefinitions>

					<!-- LEFT FILTER PANEL -->
					<Border Grid.Column="0"
                            Margin="0,0,10,0"
                            Padding="10"
                            BorderBrush="#CCCCCC"
                            BorderThickness="1">

						<StackPanel>
							<TextBlock Text="Category"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,5"/>

							<ComboBox ItemsSource="{Binding Categories}"
                                      SelectedItem="{Binding SelectedCategory}"
                                      Width="160"/>
						</StackPanel>
					</Border>

					<!-- STOCK ITEMS GRID -->
					<DataGrid Grid.Column="1"
                              ItemsSource="{Binding ItemsView}"
                              SelectedItem="{Binding SelectedItem}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True" Grid.ColumnSpan="3">

						<DataGrid.Columns>

							<DataGridTextColumn Header="Item Code"
                                                Binding="{Binding ItemCode}" />

							<DataGridTextColumn Header="Description"
                                                Binding="{Binding Description}" />

							<DataGridTextColumn Header="Qty"
                                                Binding="{Binding Quantity}" />

							<DataGridTextColumn Header="Unit"
                                                Binding="{Binding Unit}" />

							<DataGridTextColumn Header="Min"
                                                Binding="{Binding MinLevel}" />

							<DataGridTextColumn Header="Max"
                                                Binding="{Binding MaxLevel}" />

							<DataGridTextColumn Header="Reorder Qty"
                                                Binding="{Binding SuggestedReorderQuantity}" />

							<DataGridTextColumn Header="Category"
                                                Binding="{Binding Category}"
                                                Width="*" />

						</DataGrid.Columns>

						<!-- ROW STATUS COLOURING -->
						<DataGrid.RowStyle>
							<Style TargetType="DataGridRow">
								<Style.Triggers>

									<!-- OUT OF STOCK -->
									<DataTrigger Binding="{Binding IsOutOfStock}" Value="True">
										<Setter Property="Background" Value="#FFDD6B6B"/>
										<Setter Property="Foreground" Value="Black"/>
									</DataTrigger>

									<!-- LOW STOCK -->
									<DataTrigger Binding="{Binding IsLowStock}" Value="True">
										<Setter Property="Background" Value="#FFFFE08A"/>
										<Setter Property="Foreground" Value="Black"/>
									</DataTrigger>

								</Style.Triggers>
							</Style>
						</DataGrid.RowStyle>

					</DataGrid>
				</Grid>

			</DockPanel>
		</TabItem>

		<!-- ===================== -->
		<!-- TAB 2: TRANSACTIONS -->
		<!-- ===================== -->

	</TabControl>
</UserControl>
