<Window x:Class="WeldAdminPro.UI.Views.ConsumableTransactionHistoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Consumable Transaction History"
        Height="500"
        Width="900"
        WindowStartupLocation="CenterOwner">

	<Grid Margin="10">
		<DataGrid ItemsSource="{Binding Transactions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

			<DataGrid.Columns>
				<DataGridTextColumn Header="Date"
                                    Binding="{Binding TransactionDate, StringFormat=yyyy-MM-dd}"
                                    Width="120"/>

				<DataGridTextColumn Header="Item Code"
                                    Binding="{Binding ItemCode}"
                                    Width="120"/>

				<DataGridTextColumn Header="Description"
                                    Binding="{Binding ItemDescription}"
                                    Width="*"/>

				<DataGridTextColumn Header="Type"
                                    Binding="{Binding Type}"
                                    Width="80"/>

				<DataGridTextColumn Header="Qty"
                                    Binding="{Binding QuantityDisplay}"
                                    Width="80"/>

				<DataGridTextColumn Header="Running Balance"
                                    Binding="{Binding RunningBalance}"
                                    Width="120"/>

				<DataGridTextColumn Header="Reference"
                                    Binding="{Binding Reference}"
                                    Width="150"/>
			</DataGrid.Columns>

		</DataGrid>
	</Grid>
</Window>
