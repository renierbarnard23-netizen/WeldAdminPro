<Window x:Class="WeldAdminPro.UI.Views.StockTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

	<StackPanel Margin="20" Width="300">

		<TextBlock Text="Item"/>
		<TextBox Text="{Binding Item.ItemCode}"
                 IsReadOnly="True"
                 Margin="0,0,0,10"/>

		<TextBlock Text="Quantity"/>
		<TextBox Text="{Binding QuantityText}"
                 Margin="0,0,0,10"/>

		<!-- ✅ Project Dropdown (Only visible for Stock OUT) -->
		<StackPanel Margin="0,0,0,10"
                    Visibility="{Binding IsStockOut,
                                 Converter={StaticResource BooleanToVisibilityConverter}}">

			<TextBlock Text="Project"/>
			<ComboBox ItemsSource="{Binding Projects}"
                      SelectedItem="{Binding SelectedProject}"
                      DisplayMemberPath="ProjectName"/>
		</StackPanel>

		<TextBlock Text="Reference"/>
		<TextBox Text="{Binding Reference}"
                 Margin="0,0,0,15"/>

		<StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">

			<Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="80"
                    Margin="0,0,10,0"/>

			<Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="80"/>
		</StackPanel>

	</StackPanel>
</Window>
