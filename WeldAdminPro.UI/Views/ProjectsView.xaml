<UserControl x:Class="WeldAdminPro.UI.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WeldAdminPro.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ProjectsViewModel />
    </UserControl.DataContext>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBox x:Name="SearchBox" Width="240" Margin="0,0,8,0" TextChanged="OnSearchTextChanged" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
            <Button Content="Export Excel" Command="{Binding ExportExcelCommand}" />
        </StackPanel>

        <!-- bind SelectedItem so selection will persist/restore via the viewmodel -->
        <DataGrid x:Name="ProjectsGrid" Grid.Row="1" ItemsSource="{Binding Projects}" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectedItem="{Binding SelectedProject, Mode=TwoWay}" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Project #" Binding="{Binding ProjectNumber}" Width="150" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Client" Binding="{Binding Client}" Width="200" />
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat=d}" Width="130" />
                <DataGridTextColumn Header="Databook" Binding="{Binding DatabookVersion}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
