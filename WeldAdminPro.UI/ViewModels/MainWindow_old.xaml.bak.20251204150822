<Window x:Class="WeldAdminPro.UI.ViewModels.MainWindow_old"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WeldAdminPro.UI.ViewModels"
        Title="MainWindow_old" Height="450" Width="800">

    <!-- Move InputBindings onto the Window element (Window.InputBindings) instead
         of placing them on a Grid. The MC3015 error occurs when Window.InputBindings
         is used as an attached property on a Grid. -->
    <Window.InputBindings>
        <!-- example keybinding; adjust Command to an ICommand on your viewmodel if needed -->
        <KeyBinding Key="N" Modifiers="Control" Command="{x:Static ApplicationCommands.New}" />
    </Window.InputBindings>

    <Grid>
        <!-- Keep this file minimal — it's a safe placeholder that compiles. Replace
             the inner layout with your real content when you're ready. -->
        <TextBlock Text="MainWindow_old placeholder" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</Window>
