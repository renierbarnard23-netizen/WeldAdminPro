<Window x:Class="WeldAdminPro.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:WeldAdminPro.UI.ViewModels"
        Title="WeldAdmin Pro" Height="700" Width="1100">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
<Border Grid.Column="0" Background="#FF2C3E50" Padding="12"><StackPanel>
            <TextBlock Text="WeldAdmin Pro" Foreground="White" FontSize="18" Margin="6"/>
            <Button Content="Projects" Command="{Binding ShowProjectsCommand}" Margin="6"/>
            <Button Content="Databook" Command="{Binding ShowDatabookCommand}" Margin="6"/>
            <Button Content="QCPs" Command="{Binding ShowQcpCommand}" Margin="6"/>
            <Button Content="Inspections" Command="{Binding ShowInspectionsCommand}" Margin="6"/>
            <Button Content="Reports" Command="{Binding ShowReportsCommand}" Margin="6"/>
        </StackPanel></Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" />
    </Grid>
</Window>
