# Copy to .env and adjust as needed
FLASK_SECRET_KEY=change-me-please
DATABASE_URL=  # leave blank to use Documents\WeldAdminProData\weldadmin.db
# Optional SMTP for email reminders (leave blank to disable email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USE_TLS=1
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=notifications@example.com
REMINDER_DAYS_AHEAD=30

# Optional: Tesseract OCR path for Windows (install from UB Mannheim build)
TESSERACT_PATH=
# Optional: Poppler bin path for PDF->image OCR (download Poppler for Windows)
POPPLER_PATH=
