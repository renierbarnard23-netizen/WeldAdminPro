@using MudBlazor
@using WeldAdminPro.Core.Quality.Models
@using WeldAdminPro.Web.Components.Shared.Production

@if (Snapshot != null)
{
    <MudGrid>

        <MudItem xs="12" sm="6" md="4" lg="2">
            <EnterpriseKpiCard Title="Compliance"
                               Value="@($"{Snapshot.ComplianceScore}%")"
                               Status="ISO 3834"
                               ChipColor="Color.Success"
                               Icon="@Icons.Material.Filled.Verified"
                               IconColor="Color.Success" />
        </MudItem>

        <MudItem xs="12" sm="6" md="4" lg="2">
            <EnterpriseKpiCard Title="Active WPS"
                               Value="@Snapshot.ActiveWps.ToString()"
                               Status="Approved"
                               ChipColor="Color.Primary"
                               Icon="@Icons.Material.Filled.Description"
                               IconColor="Color.Primary" />
        </MudItem>

        <MudItem xs="12" sm="6" md="4" lg="2">
            <EnterpriseKpiCard Title="Qualified Welders"
                               Value="@Snapshot.QualifiedWelders.ToString()"
                               Status="Certified"
                               ChipColor="Color.Success"
                               Icon="@Icons.Material.Filled.Groups"
                               IconColor="Color.Success" />
        </MudItem>

        <MudItem xs="12" sm="6" md="4" lg="2">
            <EnterpriseKpiCard Title="Pending NDT"
                               Value="@Snapshot.PendingNdt.ToString()"
                               Status="Awaiting"
                               ChipColor="Color.Warning"
                               Icon="@Icons.Material.Filled.Search"
                               IconColor="Color.Warning" />
        </MudItem>

        <MudItem xs="12" sm="6" md="4" lg="2">
            <EnterpriseKpiCard Title="Open Repairs"
                               Value="@Snapshot.OpenRepairs.ToString()"
                               Status="Outstanding"
                               ChipColor="Color.Error"
                               Icon="@Icons.Material.Filled.Build"
                               IconColor="Color.Error" />
        </MudItem>

        <MudItem xs="12" sm="6" md="4" lg="2">
            <EnterpriseKpiCard Title="Expiring"
                               Value="@Snapshot.ExpiringQualifications.ToString()"
                               Status="30 Days"
                               ChipColor="Color.Warning"
                               Icon="@Icons.Material.Filled.Schedule"
                               IconColor="Color.Warning" />
        </MudItem>

    </MudGrid>
}

@code
{
    [Parameter]
    public QualitySnapshot? Snapshot { get; set; }
}