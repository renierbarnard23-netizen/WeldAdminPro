name: Build WeldAdmin Pro Installer

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  installer:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore NuGet packages
      run: dotnet restore WeldAdminPro.sln

    - name: Build solution (Release)
      run: dotnet build WeldAdminPro.sln --configuration Release --no-restore

    - name: Install Inno Setup
      run: choco install innosetup --yes

    - name: Build installer
      shell: pwsh
      run: |
        Write-Host "Current directory:"
        Get-Location

        Write-Host "Listing repo root:"
        Get-ChildItem

        if (!(Test-Path ".\weldadmin_installer.iss")) {
          throw "weldadmin_installer.iss not found in repo root"
        }

        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" ".\weldadmin_installer.iss"

    - name: Upload installer artifact
      uses: actions/upload-artifact@v4
      with:
        name: WeldAdminPro-Installer
        path: Output/*.exe
